<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="tests/bootstrap.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <!--
        Database credentials (DB_*) are not set here: they come from .env (loaded in tests/bootstrap.php
        before this file merges) or from the shell environment (CI). Use PostgreSQL with PostGIS for
        GIS/listings mirror tests. Dedicated names `idx_api_testing` or `testing` are whitelisted in
        tests/TestCase.php; any other database name requires ALLOW_DESTRUCTIVE_TEST_DB=true because
        many suites use RefreshDatabase (destructive migrations).
    -->
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <env name="CACHE_STORE" value="array"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="NIGHTWATCH_ENABLED" value="false"/>
        <env name="BRIDGE_API_KEY" value="test-bridge-key"/>
        <env name="BRIDGE_HOST" value="https://bridge.test"/>
        <!-- Override .env: dev machines may set ALLOW_DESTRUCTIVE_DB_COMMANDS=true for maintenance; tests must assert guard refusal. -->
        <env name="ALLOW_DESTRUCTIVE_DB_COMMANDS" value="false" force="true"/>
        <env name="IDX_PLATFORM_HOSTS" value="localhost,127.0.0.1,dev-idx.quantyralabs.cc,idx.quantyralabs.cc,staging-idx.quantyralabs.cc"/>
        <env name="IDX_API_HOSTS" value="localhost,staging-idx-api.quantyralabs.cc"/>
    </php>
</phpunit>
